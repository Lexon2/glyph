# Copy to keystore.properties (gitignored) and fill in real values.
# Generate a keystore: scripts/generate-release-keystore.ps1
storeFile=signing/release.keystore
storePassword=CHANGE_ME
keyAlias=glyph
keyPassword=CHANGE_ME
